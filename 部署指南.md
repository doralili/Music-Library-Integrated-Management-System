# 音乐库管理系统从零复现指南

这篇文档将指导你如何在全新的 Windows 环境下，从安装底层运行环境（WSL + Docker）开始，一步步启动并复现本数据库系统大作业。

---

## 步骤一：安装 WSL (Windows 的 Linux 子系统)

由于 Docker 在 Windows 上依赖 WSL2 运行，因此我们需要先安装它。

1. 右键点击你的 Windows **“开始”** 菜单按钮，选择 **“Windows PowerShell (管理员)”** 或 **“终端 (管理员)”**。
2. 在弹出的蓝底或黑底黑框中，输入以下命令并回车：
   ```powershell
   wsl --install
   ```
3. 等待进度条走完（这会自动为你安装 Ubuntu 系统内核）。
4. **必做：** 安装完成后，**重启你的电脑**。
5. 重启后系统可能会弹出一个 Ubuntu 的黑色命令行窗口，让你设置初始用户名和密码，随便设置一个即可。

---

## 步骤二：安装 Docker Desktop

数据库 (openGauss) 将直接运行在 Docker 容器中，这样不会弄脏你的电脑环境。

1. 浏览器打开 Docker 官网下载页面：[Docker Desktop for Windows](https://docs.docker.com/desktop/install/windows-install/)
2. 点击 **"Download for Windows"**，下载安装包 (`Docker Desktop Installer.exe`)。
3. 双击运行安装包，一路保持默认勾选（特别是要确保勾选了 **"Use WSL 2 instead of Hyper-V"**）。
4. 安装完成后启动 Docker Desktop（可能需要你简单注册登录，或直接跳过）。
5. 看到 Docker 界面左下角亮起 **绿色的图标 (Engine running)**，说明 Docker 已经就绪。

---

## 步骤三：一键部署 openGauss 数据库

有了 Docker，我们不需要繁琐的物理安装配置即可启动 openGauss 数据库服务端。

1. 打开 VS Code 或任意 PowerShell 终端。
2. 输入以下命令拉取并启动数据库容器（注意密码 `Yourpassword@9830` 必须满足复杂度要求）：
   ```powershell
   docker run --name my_opengauss -p 5432:5432 -e GS_PASSWORD=Axmo@9830 -d docker.m.daocloud.io/enmotech/opengauss:latest
   ```
   *(注：这里使用了 daoCloud 源 `docker.m.daocloud.io` 加速拉取，如果在国内网速慢也可以替换成其他镜像加速源)*
3. 等待几分钟让数据库启动。你可以使用 `docker ps` 命令查看状态。

---

## 步骤四：配置 Python 环境

数据库后端已经跑起来了，现在我们需要配置前端交互的 Python 环境。

1. 确保你的电脑已经安装了 **Python (建议 3.8 ~ 3.12 版本)**。
2. 使用 VS Code 打开本项目文件夹 (`poj1`)。
3. 在 VS Code 的终端中，安装项目所需的依赖包（包括 psycopg2 数据库驱动、Streamlit UI 框架、Werkzeug 密码加密库）：
   ```powershell
   pip install -r requirements.txt
   ```
   *(如果不使用依赖文件，也可以手动安装：`pip install streamlit psycopg2-binary werkzeug`)*

---

## 步骤五：数据库建表与初始化数据

刚建立的 openGauss 数据库是空的，你需要运行初始化脚本建立歌曲表、用户表等结构，并插入测试数据。

1. 在 VS Code 终端中，确保你处于项目根目录，运行：
   ```powershell
   python db_init.py
   ```
2. 如果控制台输出建表成功、触发器建立成功、视图创建成功等绿色的 `✅` 提示，说明数据库初始化完全成功。

---

## 步骤六：启动系统前端

底层与数据均准备就绪，可以启动图形化用户界面了！

1. 在 VS Code 终端中输入：
   ```powershell
   streamlit run app.py
   ```
2. 运行后，浏览器会自动弹出一个新标签页（或者你手动点击终端里的 `http://localhost:8501`）。
3. **欢迎来到音乐库管理系统！**
   - 默认超级管理员账号：`admin`
   - 默认密码：`admin123`
   - 测试账号密码统一为：`123456`

---

## 新用户最短部署与测试流程

如果你是第一次接触这个项目，可以直接按下面这组最短命令完成部署与测试。

### 1. 进入项目目录

```powershell
cd "C:\Users\27726\Documents\lxt\Learning\Sophomore\Second term\数据库系统\数据库系统\poj1"
```

### 2. 启动 openGauss 容器

如果本机还没有数据库容器，执行：

```powershell
docker run --name my_opengauss -p 5432:5432 -e GS_PASSWORD=Axmo@9830 -d docker.m.daocloud.io/enmotech/opengauss:latest
```

如果之前已经创建过该容器，只需要执行：

```powershell
docker start my_opengauss
```

### 3. 创建数据库用户 `myadmin`

进入数据库命令行：

```powershell
docker exec -it my_opengauss gsql -U omm -W Axmo@9830 -d postgres
```

进入 `gsql` 后执行：

```sql
CREATE USER myadmin WITH PASSWORD 'Axmo@9830';
ALTER USER myadmin SYSADMIN;
\q
```

### 4. 安装依赖

```powershell
pip install -r requirements.txt
```

### 5. 初始化数据库

```powershell
python db_init.py
```

### 6. 创建默认管理员账号

```powershell
python auth.py
```

### 7. 启动系统

```powershell
streamlit run app.py
```

### 8. 打开页面并测试

在浏览器中打开：

```text
http://localhost:8501
```

使用默认管理员账号登录：

- 用户名：`admin`
- 密码：`admin123`

建议按下面顺序完成基础测试：

1. 注册一个新用户并确认可以正常登录。
2. 使用 `admin / admin123` 登录，进入系统管理页，确认能看到该新用户。
3. 将该新用户提升为 `music_admin`，重新登录后验证其可以进入曲库管理页。
4. 使用普通用户测试搜索、歌单、评论、论坛与个人资料功能。
5. 使用系统管理员测试新增用户、修改角色、重置密码、删除用户。

---

## 常见排错指南 (Troubleshooting)

- **连不上数据库 ( psycopg2.OperationalError )**
  请确保 Docker Desktop 正在运行，并且在 Docker 界面中能看到 `my_opengauss` 容器状态为 `Running`。如果容器停了，可以点击 Start 手动启动。
  
- **本地图片/音频上传后找不到**
  系统运行时会自动在项目目录下生成 `avatars/`（存头像）和 `audios/`（存本地 mp3）这两个文件夹，请不要通过只读或无权限的系统敏感目录运行本项目。
