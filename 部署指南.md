# 音乐库管理系统从零复现指南

本文档用于指导老师或同学在一台新的 Windows 电脑上复现本数据库课程设计项目。项目基于 Python、Streamlit 和 openGauss，数据库运行在 Docker 容器中。

建议先将提交的压缩包解压，然后在 VS Code 或 PowerShell 中进入解压后的项目根目录。后续命令默认都在项目根目录下执行。

---

## 一、安装基础环境

### 1. 安装 WSL

Docker Desktop 在 Windows 上通常依赖 WSL2 运行。

1. 右键点击 Windows “开始” 菜单，选择 “Windows PowerShell（管理员）” 或 “终端（管理员）”。
2. 执行：

   ```powershell
   wsl --install
   ```

3. 安装完成后重启电脑。
4. 如果重启后弹出 Ubuntu 命令行窗口，按提示设置用户名和密码即可。

### 2. 安装 Docker Desktop

1. 打开 Docker 官方下载页面：https://docs.docker.com/desktop/install/windows-install/
2. 下载并安装 Docker Desktop for Windows。
3. 安装时保持默认选项，建议勾选 “Use WSL 2 instead of Hyper-V”。
4. 启动 Docker Desktop，等待左下角显示 Engine running。

### 3. 安装 Python

请安装 Python 3.8 到 3.12 之间的版本，并确保 `python` 和 `pip` 可以在 PowerShell 中直接使用。

---

## 二、启动 openGauss 数据库容器

在项目根目录下打开 PowerShell，执行：

```powershell
docker run --name my_opengauss -p 5432:5432 -e GS_PASSWORD=Axmo@9830 -d docker.m.daocloud.io/enmotech/opengauss:latest
```

如果电脑中已经创建过同名容器，只需要启动它：

```powershell
docker start my_opengauss
```

等待数据库启动后，可用以下命令检查容器状态：

```powershell
docker ps
```

---

## 三、创建数据库用户

进入 openGauss 命令行：

```powershell
docker exec -it -u omm my_opengauss bash -lc "source /home/omm/.bashrc; gsql -U omm -W Axmo@9830 -d postgres"
```

进入 `gsql` 后执行：

```sql
CREATE USER myadmin WITH PASSWORD 'Axmo@9830';
ALTER USER myadmin SYSADMIN;
\q
```

如果提示用户已存在，可以忽略该提示，继续后面的步骤。

---

## 四、安装 Python 依赖

在项目根目录下执行：

```powershell
pip install -r requirements.txt
```

依赖文件会安装 Streamlit、psycopg2、Werkzeug、pydub、mutagen 等项目运行所需库。

---

## 五、恢复或初始化数据库

本项目提供两种数据库准备方式，推荐优先使用方式 A。

### 方式 A：使用 `music_backup.sql` 恢复完整数据库

如果提交文件中包含 `music_backup.sql`，推荐直接恢复该备份。它包含导出时数据库中的真实表结构和数据，更接近演示时的完整状态。

1. 将备份文件复制到容器内：

   ```powershell
   docker cp music_backup.sql my_opengauss:/tmp/music_backup.sql
   ```

2. 恢复 SQL 备份：

   ```powershell
   docker exec -it -u omm my_opengauss bash -lc "source /home/omm/.bashrc; gsql -U myadmin -W Axmo@9830 -d postgres -f /tmp/music_backup.sql"
   ```

`music_backup.sql` 已包含删除旧 `music` 数据库、重新创建数据库、切换到 `music` 数据库并恢复表结构与数据的命令。恢复完成后即可启动系统。

### 方式 B：使用初始化脚本重新建库

如果没有 `music_backup.sql`，或者希望从初始数据重新生成数据库，可执行：

```powershell
python db_init.py --reset
```

该脚本会创建 `music` 数据库、建立数据表、创建默认管理员，并从 `songs_import_1000.csv` 导入初始歌曲数据。

默认管理员账号：

- 用户名：`admin`
- 密码：`admin123`

---

## 六、启动系统

在项目根目录下执行：

```powershell
streamlit run app.py
```

浏览器打开：

```text
http://localhost:8501
```

使用默认管理员账号登录：

- 用户名：`admin`
- 密码：`admin123`

---

## 七、基础测试流程

建议按以下顺序验证系统功能：

1. 注册一个新用户，并确认可以正常登录。
2. 使用 `admin / admin123` 登录，进入系统管理页，确认能看到用户列表。
3. 将新用户角色修改为 `music_admin`，重新登录后验证其可以进入曲库管理页。
4. 使用普通用户测试歌曲搜索、歌单、评论、论坛与个人资料功能。
5. 使用系统管理员测试新增用户、修改角色、重置密码、删除用户。
6. 使用音乐管理员测试歌曲新增、修改、删除和查询功能。

---

## 八、常见问题

### 1. 连接不上数据库

如果出现 `psycopg2.OperationalError`，请检查：

- Docker Desktop 是否正在运行。
- `my_opengauss` 容器是否处于 Running 状态。
- 本机 5432 端口是否被其他数据库占用。

可执行：

```powershell
docker ps
```

确认容器状态。

### 2. 容器已存在但无法重新创建

如果执行 `docker run` 时提示容器名已存在，直接启动旧容器：

```powershell
docker start my_opengauss
```

### 3. 本地图片或音频无法显示

系统使用以下本地目录保存上传资源：

- `avatars/`：用户头像。
- `audios/`：本地音频文件。
- `figures/`：报告或展示用图。

请确保这些目录随项目一起提交，并在项目根目录下运行系统。

### 4. 依赖安装失败

可先升级 pip：

```powershell
python -m pip install --upgrade pip
```

然后重新执行：

```powershell
pip install -r requirements.txt
```
